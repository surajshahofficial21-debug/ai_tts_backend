services:
  - type: web
    name: ai-tts-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    plan: free
    envVars:
      - key: PORT
        value: 5000
